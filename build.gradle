plugins {
  id "org.sonarqube" version "3.1.1"
}

sonarqube {
  properties {
    property "sonar.projectKey", "union93_FBDtoC_Inspector"
    property "sonar.organization", "union93"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
